# Models3D

Add the rigged `Character.usdz` model and separately authored exercise clips to
this folder. Self-contained `<exercise>.usdz` clips are preferred; compatible
`.dae` animation-only clips remain supported. Keep filenames synchronized with
`Model3DAnimationCatalog.filenamesByExerciseID`.
